{
  "name": "@stash-it/adapter-memory",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Add dev script here'",
    "build": "echo 'Add build script here'",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "dependencies": {
    "@stash-it/core": "workspace:*"
  }
}
